# What's new in 0.0.4

- **dsh 0.1.6 插件管理器适配**：支持 dsh 0.1.6+ 全新插件管理面板中的 `plugins.bundle.config`（Bundle 级配置，位于插件详情页）与 `plugins.row.config`（Row 级配置，行右侧「配置」按钮）。
- **向后兼容保留**：继续保留 `settings.plugin.item` 挂载，无缝兼容 dsh ≤ 0.1.5 老版本。
- **默认展开优化**：在专属配置页中默认展开表单项，方便快速调节位置、材质及刻度分布。
